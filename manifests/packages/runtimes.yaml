# Language runtimes and version managers (cross-platform)

actions:
  # Version manager
  - action: package.install
    name: mise

  # Node (needed for Claude Code, LSPs)
  - action: package.install
    name: node

  # Java
  - action: package.install
    name: openjdk
