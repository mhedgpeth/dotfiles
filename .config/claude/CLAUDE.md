# CLAUDE.md

This directory contains the global configuration for claude and is symlinked to `~/.claude/settings.json`.

## Permissions

Permissions should be explicitly defined in the `settings.json` file and documented in the `permissions.md` file. If you update one, update the other.
